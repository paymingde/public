# 分层状态机（HSM）设计精髓总结
## 一、核心定义
- **父状态机（Parent / Mode）**：上层抽象，负责**决策、模式、意图**
- **子状态机（Child / State）**：下层执行，负责**流程、动作、物理驱动**

## 二、经典分工（灵魂思想）
- **父状态机**：管「**做什么、要不要做、进入哪种大模式**」
  - 来源：用户操作、外部命令、系统调度
  - 特点：粗粒度、状态少、切换慢、偏抽象
- **子状态机**：管「**具体怎么做、一步步如何执行**」
  - 来源：父状态机指令、时序、传感器条件
  - 特点：细粒度、状态多、切换快、偏物理实现

## 三、结构与实现
1. **C 语言本质**
   分层状态机 ≈ 两层 `switch` 嵌套（或父 `switch` + 子函数内 `switch`）
2. **常规写法**
   - 父：模式判断、事件处理、状态跳转
   - 子：具体业务逻辑、硬件操作、时序控制
3. **嵌套不是缺点，而是分层的灵魂**

## 四、行业通用规范
1. **命名习惯**
   - 父状态机：`XXX_Mode_e`（IDLE、RUN、FAULT、STANDBY）
   - 子状态机：`XXX_State_e` / `XXX_SubState_e`（INIT、STARTING、RUNNING、STOPPING）
2. **IDLE 位置**
   - 优先放在**父状态机**，表示整机/模块待机
3. **工作状态用词**
   - 父：`RUN / RUNNING`
   - 子：`WORK / WORKING`

## 五、工程适用范围
- **简单家电 / 工控传感器 / 电机模块**：**2 层完全足够**
- 复杂机器人 / 汽车电子：最多 3 层
- 4 层及以上属于过度设计

## 六、底层逻辑（冯·诺依曼视角）
1. MCU 已承担运算器、控制器、存储器
2. HSM 核心是对**外部输入信息建模**
3. 输入天然分为两类：
   - 意图类输入 → 父状态机
   - 过程类输入 → 子状态机
4. 因此**HSM 至少两层是结构必然，不是人为约定**

## 七、一句话精髓
**父状态机定方向，子状态机走步骤；
父管决策意图，子管物理执行；
两层分离，是复杂工业逻辑不乱的核心架构。**
